import { Box, Icon } from '@chakra-ui/react';
import React from 'react';

interface Props {
  
}

const Save: React.FC<Props> = ({  }) => {
  return (
    <Box>
        <Icon/>
    </Box>
  );
};

export default Save;