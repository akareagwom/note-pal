import { Box } from '@chakra-ui/react';
import React from 'react';

interface Props {
  
}

const Save: React.FC<Props> = ({  }) => {
  return (
    <Box>
      
    </Box>
  );
};

export default Save;